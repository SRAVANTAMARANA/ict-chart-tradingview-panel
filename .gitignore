# Ignore local env
.env

# Python
__pycache__/
*.py[cod]
*.pyo

# Byte-compiled / optimized / DLL files
*.py[cod]

# Docker
# (keep docker-compose overrides ignored, but stop ignoring all Dockerfiles so frontend/backend Dockerfiles are tracked)
**/docker-compose.override.yml

# Node / build artifacts
node_modules/
dist/
